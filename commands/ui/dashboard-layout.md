---
name: dashboard-layout
description: Generate dashboard layout patterns — sidebar navigation, KPI cards, chart grids, data tables, collapsible panels, and responsive data-dense layouts.
---

Generate dashboard layouts. Cover: collapsible sidebar (icon-only collapsed state), top header with breadcrumbs, main content with responsive grid, KPI/metric cards with trend indicators, chart placement zones, data table areas, filter bars. Include mobile-responsive behavior (sidebar becomes bottom sheet or hamburger). Use CSS Grid for the main layout, Flexbox for component internals. Provide dark mode variant. Use shadcn/ui components. Accept $ARGUMENTS as dashboard type.
