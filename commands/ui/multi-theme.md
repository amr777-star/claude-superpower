---
name: multi-theme
description: Generate multi-theme architectures — CSS custom property based theme switching, light/dark/high-contrast themes, brand overlays, user-customizable themes, and smooth transition animations.
---

Generate multi-theme system. Cover: CSS custom property layering (base → brand → theme → component), light/dark/high-contrast themes, theme switching with React context, system preference detection (prefers-color-scheme), localStorage persistence, smooth theme transition (transition on color properties), brand-specific overlays for white-labeling, user-customizable accent colors. Use data-theme attribute pattern. Prevent flash of wrong theme (script in head). Accept $ARGUMENTS as theme approach.
